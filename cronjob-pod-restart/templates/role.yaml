apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "cronjob-pod-restart.clusterRole" . }}
rules:
- apiGroups:
  - ""
  resources:
  - "pods"
  verbs:
  - "list"
  - "delete"
  - "get"